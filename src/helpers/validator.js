export const MovementValidator = (move, head) => {
  let ret = true;

  return { validMove: true, newHead: 58 };
};

// LeftMoveValidator
// RightMoveValidator
// UpMoveValidator
// DownMoveValidator
